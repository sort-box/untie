<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!--
	  App Sandbox variant. This is the configuration the PRD's "security-scoped
	  bookmark persistence" (issue W4) implies: the app can only touch what the
	  user explicitly picks in NSOpenPanel, and it persists that access across
	  relaunches by saving an APP-SCOPED security-scoped bookmark.

	  App-scoped security-scoped bookmarks (URL.bookmarkData(.withSecurityScope)
	  + startAccessingSecurityScopedResource) are ONLY available when the App
	  Sandbox is enabled together with files.bookmarks.app-scope.

	  Electron + App Sandbox is the risky combination this spike must validate on
	  a real signed build (helper processes, native modules, and library
	  validation frequently need extra work). Swap electron-builder.yml's
	  `entitlements`/`entitlementsInherit` to this file (and inherit) to test it.
	-->
	<key>com.apple.security.app-sandbox</key>
	<true/>
	<key>com.apple.security.files.user-selected.read-write</key>
	<true/>
	<key>com.apple.security.files.bookmarks.app-scope</key>
	<true/>
	<key>com.apple.security.cs.allow-jit</key>
	<true/>
	<key>com.apple.security.cs.allow-unsigned-executable-memory</key>
	<true/>
	<key>com.apple.security.cs.disable-library-validation</key>
	<true/>
</dict>
</plist>
